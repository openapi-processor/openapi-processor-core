get:
  tags:
    - endpoint
  parameters:
    - in: query
      name: min
      schema:
        type: array
        minItems: 2
        items:
          type: string
    - in: query
      name: max
      schema:
        type: array
        maxItems: 4
        items:
          type: string
    - in: query
      name: min-max
      schema:
        type: array
        minItems: 2
        maxItems: 4
        items:
          type: string
  responses:
    '204':
      description: empty
